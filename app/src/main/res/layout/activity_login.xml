<?xml version="1.0" encoding="utf-8"?>
<!--
    ScrollView handles the keyboard pushing content up on small screens.
    Inner ConstraintLayout fills the screen and vertically centers the card.
    The card (LinearLayout) is 0dp width constrained to max 480dp — fills on
    phone, caps and centers on tablet/desktop. Nothing stretches.
-->
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_white"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="@dimen/screen_min_height">

        <!--
            Content card: max 480dp, centered horizontally.
            vertical_bias=0.42 positions it slightly above center — natural for login screens.
        -->
        <LinearLayout
            android:id="@+id/loginCard"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:paddingTop="48dp"
            android:paddingBottom="48dp"
            app:layout_constraintWidth_max="480dp"
            app:layout_constraintWidth_default="spread"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_bias="0.42">

            <!-- Logo: fixed dimen width (130dp phone / 170dp tablet), aspect ratio via adjustViewBounds -->
            <ImageView
                android:id="@+id/loginLogo"
                android:layout_width="@dimen/login_logo_width"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:src="@drawable/ic_logo_full"
                android:contentDescription="@string/logo_description"
                android:scaleType="fitCenter"
                android:adjustViewBounds="true"
                android:layout_marginBottom="40dp" />

            <!-- Username label -->
            <TextView
                android:id="@+id/labelUsername"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/label_username"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:layout_marginBottom="4dp" />

            <!-- Username input -->
            <EditText
                android:id="@+id/inputUsername"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:hint="@string/hint_username"
                android:textColorHint="@color/text_hint"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                android:background="@drawable/bg_search_bar"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:inputType="text"
                android:singleLine="true"
                android:layout_marginBottom="16dp" />

            <!-- Password label -->
            <TextView
                android:id="@+id/labelPassword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/label_password"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:layout_marginBottom="4dp" />

            <!-- Password row (input + eye toggle) -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginBottom="24dp">

                <EditText
                    android:id="@+id/inputPassword"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:hint="@string/hint_password"
                    android:textColorHint="@color/text_hint"
                    android:textSize="14sp"
                    android:textColor="@color/text_primary"
                    android:background="@drawable/bg_search_bar"
                    android:paddingStart="12dp"
                    android:paddingEnd="48dp"
                    android:inputType="textPassword"
                    android:singleLine="true" />

                <ImageButton
                    android:id="@+id/btnTogglePassword"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_gravity="end|center_vertical"
                    android:layout_marginEnd="4dp"
                    android:src="@drawable/ic_eye"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Toggle password visibility" />

            </FrameLayout>

            <!-- Sign In button -->
            <Button
                android:id="@+id/btnSignIn"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="@string/btn_sign_in"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:letterSpacing="0.08"
                android:backgroundTint="@color/green_primary"
                android:layout_marginBottom="12dp" />

            <!-- Create Account button -->
            <Button
                android:id="@+id/btnCreateAccount"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="@string/btn_create_account"
                android:textColor="@color/text_primary"
                android:textSize="12sp"
                android:letterSpacing="0.06"
                style="@style/Widget.Material3.Button.OutlinedButton"
                app:strokeColor="@color/text_hint" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>
